# Visual Studio / editor metadata
.vs/
.idea/
*.user

# build output
bin/
obj/
publish/
_site/
_pubtmp/
# NOTE: Showroom/dist/ is NOT ignored -- it's the committed, pre-built publish output CI
# deploys from directly (see .github/workflows/deploy.yml). Deliberate exception.

# OS cruft
.DS_Store
Thumbs.db
